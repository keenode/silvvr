{#
  Copy Block
  ----------------------------------------------------------------------
  app/view/component/copy-block
  @author Keenan Staffieri
#}

{# Namespace #}
{% set ns = 'component-copy-block' %}

{# Component Reference ID #}
{% set componentRef = 'copy-block' %}

{# Component Name #}
{% set componentName = 'Copy Block' %}

{# Extend base component, bootstrap variables #}
{% extends 'component/_base/component.njk' %}

{# BEGIN: Component definition block ----------------------------- #}
{% block component %}

<div class="{{ns}}__container">

  <h2 class="{{ns}}__heading">
    {{ opts.heading | safe }}
  </h2>

  <div class="{{ns}}__content">
    {{ opts.content | safe }}
  </div>

</div> {# __container #}

{% endblock %}
{# END: Component definition block ----------------------------- #}
